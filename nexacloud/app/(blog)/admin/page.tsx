import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Admin' };

export default function AdminPage() {
  return (
    <div>
      <p>Admin dashboard — coming in Task 14</p>
    </div>
  );
}
