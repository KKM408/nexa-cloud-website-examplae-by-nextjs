import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Posts' };

export default function PostsPage() {
  return (
    <div>
      <p>Posts listing — coming in Task 10</p>
    </div>
  );
}
