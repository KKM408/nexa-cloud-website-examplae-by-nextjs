import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Zhang.dev — Full-Stack Development Blog',
};

export default function HomePage() {
  return (
    <div>
      <p>Home page — coming in Task 9</p>
    </div>
  );
}
